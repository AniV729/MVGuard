<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Multivariate Cryptography</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f0f0f0;
        }
        .container {
            width: 400px;
            padding: 20px;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        input[type="text"],
        textarea {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        button {
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Multivariate Cryptography</h1>
        <form method="post">
            <textarea name="message" placeholder="Enter text to encrypt..."></textarea>
            <button type="submit" name="encrypt">Encrypt</button>
        </form>
        {% if encrypted %}
        <h2>Encrypted Message:</h2>
        <textarea readonly>{{ encrypted }}</textarea>
        {% endif %}
        <hr>
        <form method="post">
            <textarea name="encrypted_message" placeholder="Enter encrypted text to decrypt..."></textarea>
            <button type="submit" name="decrypt">Decrypt</button>
        </form>
        {% if decrypted %}
        <h2>Decrypted Message:</h2>
        <textarea readonly>{{ decrypted }}</textarea>
        {% endif %}
        {% if error %}
        <p style="color: red;">{{ error }}</p>
        {% endif %}
    </div>
</body>
</html>
