<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Patients</title>
</head>
<body>
    <h2>Patients</h2>
    <ul>
    {% for patient in patients %}
        <li><a href="{{ url_for('decrypt_patient', id=patient.id) }}">{{ patient.name }}</a></li>
    {% endfor %}
    </ul>
</body>
</html>